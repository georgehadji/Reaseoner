[pytest]
testpaths = tests
pythonpath = src
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
    timeout: marks tests with a timeout threshold (requires pytest-timeout)
filterwarnings =
    ignore::DeprecationWarning:.*simpleeval.*

